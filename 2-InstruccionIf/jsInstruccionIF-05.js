function mostrar()
{
	//tomo la edad  
	let vEdad;
	
	vEdad = parseInt(document.getElementById("txtIdEdad").value);
	
	//if(!(vEdad >=13&& vEdad <=17))
	if (vEdad <13|| vEdad >17)
        { 
		alert ("No es adolescente");
	    }
// if (vEdad <13|| vEdad >17)
}//FIN DE LA FUNCIÓN